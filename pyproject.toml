[project]
name = "leaves-disease-classification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AbdelRahmanAlTamimi", email = "tamimi6030@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
    "scikit-learn>=1.9.1",
]

[project.scripts]
leaves-disease-classification = "leaves_disease_classification:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "dvc>=3.67.1",
    "pytest>=9.1.1",
]
